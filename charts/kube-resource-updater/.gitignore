# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Helm packaging artifacts
*.tgz
# …except the vendored subchart (self-contained offline renders/CI)
!charts/*.tgz

# IDE
.idea/
.vscode/

# Local Claude assistant context — kept out of git so contributors don't
# inherit operator examples specific to one deployment. Re-add the file
# (and remove this rule) when the chart ships a public, generic version.
CLAUDE.md
